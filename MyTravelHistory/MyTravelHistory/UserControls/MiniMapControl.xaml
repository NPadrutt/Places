<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="clr-namespace:Microsoft.Phone.Maps.Controls;assembly=Microsoft.Phone.Maps" x:Class="MyTravelHistory.UserControls.MiniMapControl"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="150" d:DesignWidth="432">
    
	<Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">

		<maps:Map x:Name="myMap" Width="432" HorizontalAlignment="Left" PedestrianFeaturesEnabled="True" LandmarksEnabled="True"/>

	</Grid>
</UserControl>